import { audioEngine } from './NativeAudioEngine.js'; let attached=false; export function attachAudioUnlock(){if(attached)return;attached=true;const unlock=async()=>{await audioEngine.unlock();window.removeEventListener('pointerdown',unlock);window.removeEventListener('keydown',unlock)};window.addEventListener('pointerdown',unlock,{once:true});window.addEventListener('keydown',unlock,{once:true});}
